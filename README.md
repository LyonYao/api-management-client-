# API Manager Client

React + Vite frontend for managing systems, APIs, endpoints and relationships.

Quick start:

```bash
cd d:/lyon_study/api-manager-client
npm install
npm run dev
```

The app talks to `http://localhost:8080` by default. You can edit `src/api.js` to change the base URL.
